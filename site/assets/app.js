const q=document.querySelector('#question'),out=document.querySelector('#result');
const render=()=>{out.innerHTML=`<div class="loading">正在理解问题并生成模拟结果…</div>`;setTimeout(()=>{const d=DEMO.result;out.innerHTML=`<section class="result"><h3>${d.conclusion}</h3><div class="metrics">${d.metrics.map(x=>`<div><span>${x[0]}</span><b>${x[1]}</b></div>`).join('')}</div><h4>模拟趋势数据</h4><table><tr><th>期间</th><th>模拟指标值（亿元）</th></tr>${d.rows.map(r=>`<tr><td>${r[0]}</td><td>${r[1]}</td></tr>`).join('')}</table><details><summary>模拟技术信息</summary><p>此结果由浏览器内置的虚构数据生成，不连接后端或数据库。</p></details></section>`},450)};
DEMO.scenarios.forEach(x=>document.querySelector('#scenarios').insertAdjacentHTML('beforeend',`<button class="chip">${x}</button>`));DEMO.examples.forEach(x=>document.querySelector('#examples').insertAdjacentHTML('beforeend',`<button class="example">${x}</button>`));document.querySelectorAll('.example').forEach(b=>b.onclick=()=>{q.value=b.textContent;render()});document.querySelector('#run').onclick=()=>q.value.trim()?render():out.innerHTML='<p class="state">请先补充机构、指标或时间范围。</p>';document.querySelector('#no-data').onclick=()=>out.innerHTML='<p class="state">当前模拟条件下没有可展示的数据，请调整时间范围。</p>';document.querySelector('#failure').onclick=()=>out.innerHTML='<p class="state">模拟查询失败，请稍后重试。</p>';
