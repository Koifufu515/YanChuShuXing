# 数据质量与对账报告

Real 发布以源文件 SHA-256、随机 `run_id` 和两库 `import_manifest` 绑定。业务库、评测库与 `active_release.json` 的 run_id 和 SHA-256 不一致时，验证程序拒绝运行。

当前发布已完成：两库 manifest 一致；SQLite 完整性和外键检查通过；manifest 行数对账通过；日期连续性、每日机构指标立方体、机构指标全周期覆盖检查通过；六类查询验证通过。验收失败返回非零退出码并列出失败项。

本地正式烟雾验收规模为13家机构、21项指标、132,678条事实、10项衍生维度和200道隔离评测题，日期范围为2024-12-31至2026-04-30。异常明细只写入私有目录，公开报告只保留聚合数量。
