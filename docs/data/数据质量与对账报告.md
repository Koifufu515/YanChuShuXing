# 数据质量与对账报告

Real 发布以源文件 SHA-256、随机 `run_id` 和两库 `import_manifest` 绑定。业务库、评测库与 `active_release.json` 的 run_id 和 SHA-256 不一致时，验证程序拒绝运行。

当前发布已完成：两库 manifest 一致；外键检查通过；六类查询验证通过。导入失败不会更新 active manifest，因此旧发布继续可用。

导入摘要至少包含源工作簿 SHA-256、发布 run_id、五张表读取数、业务库写入数、评测库写入数和异常数。异常明细只写入私有目录，公开报告只保留规则编号和聚合数量。
